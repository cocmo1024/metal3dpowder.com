---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProductCard from '../components/ProductCard.astro';
import SiteFooter from '../components/SiteFooter.astro';
import SiteHeader from '../components/SiteHeader.astro';
import { siteInfo } from '../data/site';
import { products } from '../data/products';

const title = 'Metal 3D Powder | Premium AM Powder Supplier';
const description =
  'English-first export website for metal additive manufacturing powders with clear pricing, particle size, and lead time.';

const siteUrl = Astro.site ?? new URL('https://metal3dpowder.com');

const organizationSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: siteInfo.brand,
  url: siteUrl.toString(),
  email: siteInfo.email,
  description,
};

const itemListSchema = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Metal 3D Powder Product List',
  itemListElement: products.map((product, index) => ({
    '@type': 'Product',
    position: index + 1,
    name: product.name,
    description: product.description,
    image: new URL(product.image, siteUrl).toString(),
    sku: product.code,
    offers: {
      '@type': 'Offer',
      priceCurrency: 'USD',
      price: product.price,
      availability: 'https://schema.org/InStock',
      url: siteUrl.toString(),
    },
  })),
};
---

<BaseLayout title={title} description={description} schema={[organizationSchema, itemListSchema]}>
  <SiteHeader pathname={Astro.url.pathname} productsHref="#products" contactHref="#contact" />

  <main>
    <section class="section page-shell" id="products">
      <div class="products-grid">
        {products.map((product, index) => <ProductCard product={product} index={index} />)}
      </div>
    </section>

    <section class="contact-strip page-shell" id="contact" aria-labelledby="contact-title">
      <p class="contact-strip__eyebrow">Contact</p>
      <h2 id="contact-title">Email for quote and order inquiry</h2>
      <p class="contact-strip__note">
        Send material grade, particle size, target process, quantity, and destination country for faster quoting.
      </p>
      <div class="contact-strip__box">
        <input
          type="text"
          value={siteInfo.email}
          readonly
          aria-label="Sales email"
          onclick="this.select();"
        />
        <button type="button" class="button button--primary" data-copy-email>
          Copy Email
        </button>
      </div>
    </section>
  </main>

  <SiteFooter />

  <script is:inline>
    const button = document.querySelector('[data-copy-email]');
    const input = document.querySelector('input[aria-label="Sales email"]');

    if (button instanceof HTMLButtonElement && input instanceof HTMLInputElement) {
      button.addEventListener('click', async () => {
        try {
          await navigator.clipboard.writeText(input.value);
          button.textContent = 'Copied';
          window.setTimeout(() => {
            button.textContent = 'Copy Email';
          }, 1600);
        } catch {
          input.focus();
          input.select();
        }
      });
    }
  </script>
</BaseLayout>

<style>
  .section {
    padding-block: clamp(2rem, 4vw, 2.75rem) clamp(3rem, 6vw, 4rem);
  }

  .products-grid {
    display: grid;
    gap: 1.25rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .contact-strip {
    display: grid;
    justify-items: center;
    gap: 0.85rem;
    padding-bottom: 3rem;
    text-align: center;
  }

  .contact-strip__eyebrow {
    margin: 0;
    color: var(--accent);
    text-transform: uppercase;
    letter-spacing: 0.16em;
    font-size: 0.76rem;
    font-weight: 600;
  }

  .contact-strip h2 {
    margin: 0;
    font-size: clamp(1.6rem, 3vw, 2.3rem);
    letter-spacing: -0.04em;
  }

  .contact-strip__note {
    max-width: 42rem;
    margin: 0;
    color: var(--muted);
    line-height: 1.7;
  }

  .contact-strip__box {
    display: grid;
    grid-template-columns: minmax(320px, auto) auto;
    gap: 0.85rem;
    align-items: center;
  }

  .contact-strip__box input {
    min-width: 0;
    height: 3rem;
    padding: 0.8rem 1rem;
    color: var(--text);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 0.95rem;
    background: rgba(255, 255, 255, 0.04);
    text-align: center;
  }

  @media (max-width: 1100px) {
    .products-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 560px) {
    .contact-strip__box {
      grid-template-columns: 1fr;
    }
  }
</style>
